/// REST API route handlers.
/// Placeholder for Phase 6 async job architecture.
/// Phase 1 is CLI-only — the API layer is defined here for forward compatibility.
pub fn init_routes() {
    // Phase 6: axum router with POST /v1/jobs, GET /v1/jobs/:id
}
